[![Review Assignment Due Date](https://classroom.github.com/assets/deadline-readme-button-22041afd0340ce965d47ae6ef1cefeee28c7c493a6346c4f15d667ab976d596c.svg)](https://classroom.github.com/a/fu6fFS4S)
[დავალების ლინკი (Figma)](https://www.figma.com/design/LrCfXSb63whwruzhSmM6pE/Ecommerce-Website-UI-Template-Design---Dark-Theme--Community---Copy-)

ამ დავალებაში მიზანია Figma მაკეტის მიხედვით დაამატო **Hover/Focus** ეფექტები და რამდენიმე **CSS animation**.

რეკომენდებული ინსტრუმენტები:
- `transition`
- `@keyframes` + `animation`
- `::before` / `::after`
- `position` / `transform`

დამატებითი იდეები და მაგალითები იხილე: `homework-13-animations/animations.md`

---

## Nav bar / Header

1) მენიუს 3 ლინკზე (Home, Products, Contact) `:hover`-ზე შეცვალე `background-color` (გლუვი გადასვლით).

გამოიყენე `transition`.

---

2) `Shop now` ღილაკის გარშემო **უსასრულოდ** უნდა ტრიალებდეს border/outline.

გამოიყენე: `animation`, `@keyframes`, `::before` ან `::after`, `position`.

---

3) კატეგორიების ღილაკებზე (All, Mens, Womens, Kids) `:hover`-ზე შეცვალე ოდნავ `background-color`.

გამოიყენე `transition`.

---

## Elevate Your Style With Our Latest Collection

კატეგორიების ღილაკებზე (All, Mens, Womens, Kids) `:hover`-ზე შეცვალე ოდნავ `background-color`.

გამოიყენე `transition`.

---

თითოეულ პროდუქტის ქარდში, სურათზე `:hover`-ზე გაადიდე ფოტო `transform: scale(1.1)`-ით.

გამოიყენე `transition`.

---

## Have Questions? Have Answers.

კატეგორიების ღილაკებზე (All, Ordering, Shopping, Returns, Support) `:hover`-ზე შეცვალე ოდნავ `background-color`.

გამოიყენე `transition`.

---

თითოეულ კითხვაზე (FAQ item), `:hover`-ზე შეცვალე ტექსტის ფერი.

გამოიყენე `transition`.

---

## Footer

სოციალური აიქონები იმოძრაონ **ვერტიკალურად** (ზემოთ/ქვემოთ) უსასრულოდ, თანაბარი სიჩქარით.

გამოიყენე: `animation`, `translateY`, `linear`, `infinite`.

---

Footer-ში (Home და Products) ლინკებზე `:hover`-ზე:
- გაადიდე ტექსტი `scale(1.1)`-ით
- ოდნავ შეცვალე ტექსტის ფერი

გამოიყენე `transition`.

---

### სურვილისამებრ
შეგიძლია დაამატო დამატებითი ანიმაციებიც, მაგრამ ანიმაციამ არ უნდა “გადაძალოს” გვერდი — იყოს მსუბუქი, სწრაფი და UX-ს გაუმჯობესებდეს.

